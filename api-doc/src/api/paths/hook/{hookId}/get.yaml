tags:
  - api
operationId: getHookByID
summary: Hook detail
consumes:
  - applicaton/json
produces:
  - application/json
parameters:
  - in: path
    name: hookID
    description: Hook ID
    required: true
    type: string
responses:
  200:
    description: vehicle info
    schema:
      $ref: '#/definitions/Hook'
